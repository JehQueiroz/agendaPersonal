<p id="notice"><%= notice %></p>

<h1>Listing Academias</h1>
<%= image_tag 'impulsofrontal.gif', :width => '15%', :height => '15%'  %>
<hr/> 
  <%= render 'formbusca' %>
<hr/>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Endereco</th>
      <th>Telefone</th>
      <th>Cnpj</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @academias.each do |academia| %>
      <tr>
        <td><%= academia.nome %></td>
        <td><%= academia.endereco %></td>
        <td><%= academia.telefone %></td>
        <td><%= academia.cnpj %></td>
        <td><%= link_to 'Show', academia %></td>
        <td>
       <%= link_to ' ', edit_academia_path(academia),  class: "glyphicon glyphicon-pencil" %>
          <%= link_to ' ', academia, method: :delete, data: { confirm: 'Are you sure?' } , class: "glyphicon glyphicon-trash" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @funcionarios, previous_label: "Anterior", next_label: "Próxima" %>
<br/>
<br>

<%= link_to 'New Academia', new_academia_path %>
